# 📦 Instalação e Uso

Para utilizar esta suite de ferramentas no seu servidor Ubuntu:

1. **Clonar o repositório:**
   ```bash
   git clone https://github.com/zendexzen/xubuntu-sysadmin-tools.git
   cd xubuntu-sysadmin-tools
   ```

2. **Dar permissões de execução:**
   ```bash
   chmod +x *.sh
   ```

3. **Executar a Auditoria de Segurança:**
   ```bash
   ./security-audit.sh
   ```

*Nota: Alguns scripts requerem privilégios de sudo para ler logs do sistema.*
